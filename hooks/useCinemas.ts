/** Alias pedido na spec; o hook canônico do projeto é `use-cinemas`. */
export { useCinemas, useCinemas as default } from "@/hooks/use-cinemas"
export type { UseCinemasParams, UseCinemasResult } from "@/hooks/use-cinemas"
