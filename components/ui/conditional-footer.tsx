'use client'

/**
 * Footer chrome temporarily disabled while we rethink the design.
 * Restore by re-enabling the pathname gates + <Footer /> render below.
 */
export function ConditionalFooter() {
  return null
}
