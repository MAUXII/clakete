/**
 * @deprecated Prefer `useT()` / `translate()` from `@/lib/i18n`.
 * Kept so older imports of catalog helpers keep working.
 */
export {
  catalogT,
  resolveUiLocale,
  type UiLocale,
} from "@/lib/i18n"
